extend layout

block content
  
  .container
    h1 数据库采集信息表格全显示，点击<a href="/download">这里</a>下载全部数据
    :markdown
      This page is to show the all information in table
    table.table.table-hover
      thread
        tr
          td 记录编号
          td 节点号
          // td 采集时间
          td 传感器编号
          td 采集值
          // td 光照
          // td 温度
          // td 浊度
          // td 流速
          td 信息插入时间
      tbody
        // - var i = 0
        each node in data
          tr
            // td #{++i}
            td #{node.record_id}
            td #{node.node_id}
            // td #{node.sense_time}
            // td #{node.sense_type}
            td #{node.sensor_id}
            td #{node.data}
            // if node.light 
            //   td #{node.light}
            // else
            //   td N/A
            // if node.temperature
            //   td #{node.temperature}
            // else
            //   td N/A
            // if node.turibidity
            //   td #{node.turbidity}
            // else 
            //   td N/A
            // if node.flowspeed
            //   td #{node.flowspeed}
            // else 
            //   td N/A
            td #{node.insert_time}
